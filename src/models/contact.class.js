export class Contact  {

    constructor(name, lastName, emailAddress, isConnected) {

        this.name = name;
        this.lastName = lastName;
        this.emailAddress = emailAddress;
        this.isConnected =isConnected;
    }
}